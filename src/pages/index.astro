---
import BaseLayout from "../layouts/BaseLayout.astro";
import RecentNotes from "../components/RecentNotes.astro";
import { getPosts } from "../utils/notion";

const pageTitle = "seunghoonkang.dev";
const description =
  "개발자 강승훈의 개인 블로그입니다. 개발, 그 외 여러 지식을 노트로 남기며, 최근에 쓴 글을 홈에서 바로 살펴볼 수 있습니다.";

const allNotes = await getPosts();
const recentNotes = allNotes
  .toSorted(
    (a, b) => new Date(b.pubDate).getTime() - new Date(a.pubDate).getTime()
  )
  .slice(0, 9);
---

<BaseLayout pageTitle={pageTitle} description={description}>
  <div class="px-[var(--space-content)]">
    <RecentNotes notes={recentNotes} />
  </div>
</BaseLayout>
